const INITIAL_STATE = {
    room: [],
    users: [],
    currUser: null
  };
  
  export const reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };